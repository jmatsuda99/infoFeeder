# Google Alerts RSS Viewer

記事一覧から `feed_name` を削除した版です。

## 変更点

- 一覧表示の列を `published / category / title / link` に変更
- 詳細表示の見出しから `feed_name` を削除

## セットアップ

```bash
pip install -r requirements.txt
streamlit run app.py
```
